export default function SocialProof() {
  // Placeholder for future testimonials.
  // When testimonials are available, replace this with 2-3 advisor quotes
  // focused on: ease of use, consolidating tools, value of all-in-one.
  // For now, render nothing — the component exists so it can be swapped in
  // without modifying page.tsx structure.
  return null;
}
